Config = {}

-- job types to include in the officers involved query
Config.JobTypes = {
  ["leo"] = true,
  ["doj"] = true,
}

Config.CitizensIncludeLeo = true -- Include the job types above in the query for citizens
